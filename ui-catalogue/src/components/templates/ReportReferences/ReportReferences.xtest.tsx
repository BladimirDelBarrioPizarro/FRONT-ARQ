/* import React from 'react'
import { render } from '@testing-library/react'
import { ReportReferences } from './ReportReferences'

test('Test de renderizado', () => {
  const reports = []
  const handleRowClickData = jest.fn()
  const buttonsColor = 'primary'
  const buttonsVariant = 'contained'
  const checkBoxColor = 'primary'

  const renderizado = render(
    <ReportReferences
      handleRowClickData = {handleRowClickData}
      reports = {reports}
      buttonsColor={buttonsColor}
      buttonsVariant={buttonsVariant}
      checkBoxColor={checkBoxColor}
    />
  )
  expect(renderizado).not.toBe(null)
})
 */
export {}
